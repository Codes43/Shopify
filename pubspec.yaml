name: shopify
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  http: ^1.4.0
  mocktail: ^1.0.0
  google_fonts: ^6.2.1
  flutter_svg: ^2.1.0
  shared_preferences: ^2.5.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.2
  build_runner: ^2.4.6
  test: ^1.25.15

flutter:
  uses-material-design: true
  assets: 
    - assets/ 